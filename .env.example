# AWS Bedrock Configuration (Optional - fallback templates used if unset)
AWS_REGION=us-east-1
BEDROCK_MODEL_ID=anthropic.claude-3-haiku-20240307-v1:0

# Cedar CLI Path (defaults to bin/cedar.exe if present or system PATH)
# CEDAR_BIN_PATH=bin/cedar.exe
